#include <iostream>
//#include <string>
//#include <string.h>
//#include <algorithm>
#include <stdio.h>
//#include <math.h>
//#include <cstring>
//#include <sstream>
//#include <queue>

using namespace std;
/*
#define PI 3.14159265358997
#define absol(x) ((x)>(0) ? (x):(-1)*(x))
#define INF 1e8
#define MAX 1600
long long int a[MAX];

*/
int main ()
{
	puts("The 1500'th ugly number is 859963392.");
	/*
	a[0]=1;
	int prime[3]={2,3,5};
	int cnt=1;
	priority_queue<long long int> q;
	q.push(-1);
	while (cnt!=MAX)
	{
		
		for (int i=0;i<3;i++)
		{
			q.push(q.top()*prime[i]);
		}
		a[cnt++]=-1*q.top();
		while (q.top()==-1*a[cnt-1])q.pop();
	}
	int n;
	for (int i=1;i<14;i++)
		printf("%d ",a[i]);
	while (cin>>n)
		printf("%d\n",a[n]);
	*/
	return 0;

}
